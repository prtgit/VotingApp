<p id="notice"><%= notice %></p>
<h2> Voting App </h2>

<h4>List of Topics</h4>
<table class="table table-striped">
  <thead>
  <tr>
    <th>Topic</th>

    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @topic.each do |topic| %>
      <tr>

        <td><%= (Topic.find(topic.id)).name %></td>
      </tr>
  <% end %>
  </tbody>
</table>




<%= link_to 'Edit', edit_home_path(@home) %> |
<%= link_to 'Back', homes_path %>
